/** Query the authorization bearer token from the iframe URL. */
export function queryAuthToken(location) {
    const token = new URL(location.href).searchParams.get('webbit_token');
    if (!token)
        throw Error('no auth token');
    return token;
}
